# CLA signatures

One line per contributor, appended chronologically. Signing = your line here,
plus a `Signed-off-by:` trailer on every commit (`git commit -s`).

    Full Name <you@example.com> — YYYY-MM-DD — @your-github-username
